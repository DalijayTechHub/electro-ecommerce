version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Injecting environment variables"
    build:
      commands:
        - echo "Static site - no build required"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []