version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No pre-build commands"
    build:
      commands:
        - echo "Static site - no build required"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'